codecov:
  branch: iroha2-dev
  allow_coverage_offsets: True
  notify:
    require_ci_to_pass: false
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
        base: "parent"
    patch: off
