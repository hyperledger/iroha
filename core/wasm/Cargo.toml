[package]
name = "iroha_core_wasm"
version = "0.1.0"
authors = ["Iroha 2 team <https://github.com/orgs/soramitsu/teams/iroha2>"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["cdylib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
iroha_data_model = { version = "=2.0.0-pre-rc.2", path = "../../data_model", default-features = false }
parity-scale-codec = { version = "2.3.1", default-features = false }

[dev-dependencies]
webassembly-test = "0.1.0"

[workspace]
